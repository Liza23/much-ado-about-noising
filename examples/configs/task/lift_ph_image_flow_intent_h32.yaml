# @package task
defaults:
  - lift_ph_image
  - _self_

# Config A (flow intent, raw 7D mean intent) with longer horizon — image obs.
# Same as lift_ph_image_flow_intent.yaml but intent_horizon=32.
intent_conditioning: true
intent_dim: 7
intent_type: mean
intent_predictor: false
intent_horizon: 32
intent_training_mode: independent
