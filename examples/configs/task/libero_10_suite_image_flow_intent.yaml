# @package task
# LIBERO-10 suite — image observations, flow-intent (all 10 tasks)
defaults:
  - libero_10_suite_image
  - _self_

arch_variant: flow_intent
intent_conditioning: true
intent_dim: 6
intent_type: mean
intent_predictor: false
intent_horizon: 8
intent_training_mode: independent
intent_keys:
  - ee_states
decoder_uses_sampled_intent: false
decoder_curriculum_steps: 0
