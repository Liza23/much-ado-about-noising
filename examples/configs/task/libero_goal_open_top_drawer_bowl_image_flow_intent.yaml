# @package task
# LIBERO-Goal single-task: open_top_drawer_bowl — image obs, flow-intent
defaults:
  - libero_goal_open_top_drawer_bowl_image
  - _self_

arch_variant: flow_intent
intent_conditioning: true
intent_dim: 6
intent_type: mean
intent_predictor: false
intent_horizon: 8
intent_training_mode: independent
intent_keys:
  - ee_states
decoder_uses_sampled_intent: false
decoder_curriculum_steps: 0
