# @package task
# LIBERO-Goal suite — image observations, multi-task (all 10 tasks) with one-hot task ID conditioning
defaults:
  - libero_goal_suite_image
  - _self_

task_id_conditioning: true
num_tasks: 10
