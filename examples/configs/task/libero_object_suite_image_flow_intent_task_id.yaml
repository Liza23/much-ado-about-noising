# @package task
# LIBERO-Object suite — image observations, flow-intent (all 10 tasks) with one-hot task ID conditioning
defaults:
  - libero_object_suite_image_flow_intent
  - _self_

task_id_conditioning: true
num_tasks: 10
