# @package task
# LIBERO-Spatial suite — flow-intent Config A (all 10 tasks, state observations)
defaults:
  - libero_spatial_suite_state
  - _self_

arch_variant: flow_intent
intent_conditioning: true
intent_dim: 6
intent_type: mean
intent_predictor: false
intent_horizon: 8
intent_training_mode: independent
intent_keys:
  - ee_states
