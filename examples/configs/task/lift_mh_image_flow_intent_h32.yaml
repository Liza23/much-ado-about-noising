# @package task
defaults:
  - lift_mh_image
  - _self_

# Config A (flow intent, raw 7D mean intent) with longer horizon — lift-mh image obs.
# Same as lift_ph_image_flow_intent_h32.yaml but on multi-human demos.
intent_conditioning: true
intent_dim: 7
intent_type: mean
intent_predictor: false
intent_horizon: 32
intent_training_mode: independent
