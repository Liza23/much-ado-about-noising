# @package task
defaults:
  - _self_

env_name: "libero_spatial"
env_type: "state"
obs_type: "state"
abs_action: false  # LIBERO uses OSC_POSE delta actions by default

# Path to a single task's HDF5 file. Override at runtime with task.dataset_path=...
# e.g. ~/datasets/libero_spatial/pick_up_the_black_bowl_on_the_plate_demo.hdf5
dataset_path: null

# Path to the task's BDDL file for evaluation (required).
# e.g. ~/LIBERO/libero/libero/bddl_files/libero_spatial/pick_up_the_black_bowl_on_the_plate.bddl
bddl_file: null

# Observation keys in LIBERO HDF5 naming convention.
# The dataset stores compact state keys, not raw robosuite names.
obs_keys:
  - ee_states        # 6D = eef pos + axis-angle orientation
  - gripper_states   # 2D
  - joint_states     # 7D

max_episode_steps: 600

# Task parameters
obs_steps: 2
act_steps: 8
horizon: 16
num_envs: 10
save_video: false
val_dataset_percentage: 0.0

# Dimensions — resolved from the env at runtime.
# Default state dim here is 6 + 2 + 7 = 15 for the compact LIBERO state.
act_dim: 7   # OSC_POSE delta: dx dy dz droll dpitch dyaw grip
obs_dim: 15
